import styled from "styled-components";
import { background, primary, secondary80, white, borderColor, borderHover } from "../../../colors";
export const ModalStyle = styled.div`
.edit-btn{
    border: 1px solid ${borderHover};
    background: ${background};
    cursor:pointer;
    border-radius:32px;
    padding: 8px;
}
`